# BuildKit per-Dockerfile ignore file (used because the build context is the
# repo root). Whitelist approach: exclude everything except backend sources.
*
!backend/
backend/*.db
backend/*.db-journal
backend/*.db-wal
